# Synthetic import samples

`opportunities.csv` contains synthetic data only and exercises the same normalization path as an
Excel `Oppty` worksheet. Real client workbooks must stay under `data/private/`, which is ignored by
Git in its entirety.
