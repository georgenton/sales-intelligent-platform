# Local PostgreSQL owner credentials used only by Docker/migrations.
POSTGRES_DB=sales_intelligence
POSTGRES_USER=postgres
POSTGRES_PASSWORD=local_only_change_me
POSTGRES_PORT=5449

# Seed-only credential. Never use this value outside local development.
DEMO_ADMIN_PASSWORD=ChangeMe-Local-2026!
