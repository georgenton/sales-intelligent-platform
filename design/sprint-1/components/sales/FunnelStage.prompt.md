One band of `SalesFunnel`. Rarely used alone — compose the funnel instead.

Hover shows, in priority order: amount, opportunity count, at-risk amount, average days in stage and likely slippage. Conversion, probability and average deal size are secondary, in the tooltip footer. Click is a *selection*, never a navigation.
