Team roster: pipeline, commit and commit mix per seller. A thin mix bar turns amber under 25% — that is the intervention cue.

```jsx
<SellerPerformance sellers={dashboard.sellerPerformance} onSelect={openSellerPanel} />
```
