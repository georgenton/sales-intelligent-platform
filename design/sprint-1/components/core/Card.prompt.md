The container for every panel. 1px border, 16px radius, near-invisible shadow.

```jsx
<Card>
  <CardHeader title="Priority risks" subtitle="Deterministic signals requiring action" action={<a href="#">View all</a>} />
  <CardContent>…</CardContent>
</Card>
```

`tone="inverse"` for Copilot/brief surfaces. Nested rows inside a card use 12px radius and a 1px border — never a second shadow.
