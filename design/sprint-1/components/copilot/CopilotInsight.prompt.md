A proactive Copilot finding on the dark Copilot surface. Headline is a fact; expanding shows the evidence.

```jsx
<CopilotInsight tone="risk" headline="4 meaningful changes since Monday"
  items={[{ label: 'Banco ABC slipped to next quarter', value: '−$180K' }]} onItemSelect={openDrawer} />
```

Only renders correctly inside `CopilotPanel` or another `tone="inverse"` surface.
